# return = ""
root = ""                  # @dev - Merkle Root
hash_path = ["", ""]
index = ""
secret = ""                # @dev - A hidden metadata hash (private)
nullifier = ""             # @dev - Should be a Poseidon2 Hash


#########################################
# @dev - InsuranceClaimData Struct data #
#########################################

[insurance_claim_data]
# @dev - The insurance claim related input data
hospital_bill_hash_bytes = []   # @dev - The hash of the hospital bill
#hospital_bill_hash = "" # @dev - The hash of the hospital bill
hospital_bill_amount = ""       # @dev - The insurance claim related input parameters

pubkey_of_hospital_bytes = []   # @dev - The public key of the hospital

signature_from_hospital_bytes = []
#signature_from_hospital = "" # @dev - The signature from the hospital

authorized_healthcare_provider_pubkey = ""                # @dev - This public key should be validated on-chain with a on-chain registry.
expected_authorized_healthcare_provider_pubkey_hash = ""  # @dev - The expected public key hash of the authorized healthcare provider